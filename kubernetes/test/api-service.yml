---
apiVersion: v1
kind: Service
metadata:
  name: api-service
  labels:
    env: test
spec:
  type: ClusterIP
  selector:
    app: api-test
  ports:
  - name: api-ports
    port: 5000
    targetPort: 5000
